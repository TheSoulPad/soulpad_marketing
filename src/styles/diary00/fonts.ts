import { colors as c } from "./colors";

export const bodyText = {
  fontFamily: `Gaegu, serif`,
  textAlign: "left",
  color: c.text01,
};
export const headerText = {
  ...bodyText,
  color: c.text02,
};
